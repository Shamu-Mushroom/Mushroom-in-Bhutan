<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deadpool.madminiproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="Mushroom in Bhutan"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EdibleActivity"
            android:label="Edible Mushroom" />
        <activity android:name=".OysterActivity" />
        <activity android:name=".GoldenoysterActivity" />
        <activity android:name=".PineActivity" />
        <activity android:name=".AboutActivity" />
        <activity
            android:name=".NonedibleActivity"
            android:label="Non-edible Mushroom" />
        <activity android:name=".LyophyllumActivity" />
        <activity android:name=".FactsActivity" />
        <activity android:name=".HomemadeActivity" />
        <activity
            android:name=".PrequationActivity"
            android:label="Prequation Edible Mushroom" />
        <activity android:name=".PhallusActivity" />
        <activity android:name=".HomushroomActivity" />
        <activity android:name=".TeamActivity"></activity>
    </application>

</manifest>